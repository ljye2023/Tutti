#pragma once
// Moved: mock device classes are now a first-class src backend.
// Include the mock backend headers from device_manager/mock/include instead.
#include "mock_physical_device.h"
#include "mock_virtual_device.h"
#include "mock_lease_manager.h"
#include "mock_device_driver.h"
